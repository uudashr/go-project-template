# Golang Project Template

Provides
- Directory structure
- Makefile
  - Docker build
  - Binary build
  - Unit test
  - Integration test (mysql)
  - Lint (`golangci-lint`)
  - Generate mock (`mockery`)
- VS Code settings
